# Third-party notices

The Let's Infer integration is AGPL-3.0-only. Incorporated or derived upstream
components retain their original terms:

- `vllm-project/vllm`, commit
  `6e448d0ea9bf3d88d898b65449ca6dc2aec170ac`: Apache-2.0.
- `NVIDIA-NeMo/Nemotron`, commit
  `e27b37133d652f72dbf631ff8bb487df45f8f7d1`: authoritative vLLM recipe and
  NVIDIA model terms referenced by the checkpoint repositories.

The model and draft checkpoints are acquired separately and remain subject to
their upstream licenses. Exact image and checkpoint identities are recorded in
`engine/PROVENANCE.json` and `runtime.json`.
