#!/usr/bin/env python3
# SPDX-License-Identifier: AGPL-3.0-only
"""SparkInfer/vLLM Engine OCI adapter for Let's Infer protocol v2."""

from __future__ import annotations

import json
import pathlib
import sys
from collections.abc import Mapping, Sequence
from typing import Any

_root = pathlib.Path(__file__).resolve().parent
_installed_lib = _root.parent / "lib"
sys.path.insert(0, str(_installed_lib if _installed_lib.is_dir() else _root))

from engine_frontend import AdapterError, artifact_path, backend_json, expand_arguments, run


def build_command(runtime: Mapping[str, Any], port: int) -> Sequence[str]:
    return [
        "/opt/letsinfer/bin/sparkinfer-serve",
        artifact_path(runtime, runtime["model"]["artifact"]),
        runtime["logical_model"],
        "127.0.0.1",
        str(port),
        *expand_arguments(runtime),
    ]


def build_environment(runtime: Mapping[str, Any]) -> Mapping[str, str]:
    cache = runtime.get("cache")
    if (
        not isinstance(cache, Mapping)
        or cache.get("provider") != "sparkinfer-letsinfer-prefix-v1"
        or cache.get("persistent") is not True
        or cache.get("prewarm") is not False
        or cache.get("replay_output_policy") != "restored-repeat-exact"
    ):
        raise AdapterError("SparkInfer persistent cache contract is invalid")
    config = cache.get("config")
    required = {
        "durable_capacity_bytes",
        "resident_capacity_bytes",
        "ttl_seconds",
        "min_tokens",
        "direct_reads",
        "native_capacity_bytes",
        "capture_tail_tokens",
        "exact_capsules",
    }
    if not isinstance(config, Mapping) or set(config) != required:
        raise AdapterError("SparkInfer persistent cache config is invalid")
    for field in (
        "durable_capacity_bytes",
        "ttl_seconds",
        "min_tokens",
        "capture_tail_tokens",
    ):
        value = config.get(field)
        if not isinstance(value, int) or isinstance(value, bool) or value <= 0:
            raise AdapterError(f"SparkInfer cache {field} must be positive")
    for field in ("resident_capacity_bytes", "native_capacity_bytes"):
        value = config.get(field)
        if not isinstance(value, int) or isinstance(value, bool) or value < 0:
            raise AdapterError(f"SparkInfer cache {field} must be non-negative")
    if config.get("resident_capacity_bytes") != 0:
        raise AdapterError("SparkInfer cache resident capacity must be zero on unified memory")
    if config.get("native_capacity_bytes") != 0:
        raise AdapterError("SparkInfer native cache capacity must be zero")
    if config.get("direct_reads") is not True:
        raise AdapterError("SparkInfer cache direct reads must be enabled")
    if config.get("exact_capsules") is not False:
        raise AdapterError("SparkInfer dspark cache capsules must be disabled")
    transfer = {
        "kv_connector": "LetsInferPrefixConnector",
        "kv_role": "kv_both",
        "kv_connector_module_path": "letsinfer_prefix_connector.connector",
        "kv_connector_extra_config": {
            "store_root": "/root/.cache/letsinfer-prefix-store/sparkinfer",
            "capacity_bytes": config["durable_capacity_bytes"],
            "resident_capacity_bytes": config["resident_capacity_bytes"],
            "ttl_seconds": config["ttl_seconds"],
            "min_tokens": config["min_tokens"],
            "direct_reads": config["direct_reads"],
            "native_capacity_bytes": config["native_capacity_bytes"],
            "capture_tail_tokens": config["capture_tail_tokens"],
            "exact_capsules": config["exact_capsules"],
            "device_address_mode": "host-copy-no-device-registration-v1",
        },
    }
    return {
        "LETSINFER_KV_TRANSFER_CONFIG": json.dumps(
            transfer, sort_keys=True, separators=(",", ":")
        )
    }


def count_tokens(host: str, port: int, body: bytes, _model: str) -> int:
    status, value = backend_json(host, port, "POST", "/tokenize", body)
    count = value.get("count")
    tokens = value.get("tokens")
    if status != 200 or not isinstance(tokens, list):
        raise AdapterError("SparkInfer exact token count failed")
    if (
        not isinstance(count, int)
        or isinstance(count, bool)
        or count <= 0
        or count != len(tokens)
    ):
        raise AdapterError("SparkInfer returned an invalid token count")
    return count


if __name__ == "__main__":
    try:
        raise SystemExit(run("sparkinfer", build_command, build_environment, count_tokens))
    except (AdapterError, KeyError, OSError, ValueError) as error:
        print(f"engine-adapter: {error}", file=sys.stderr)
        raise SystemExit(1) from error
